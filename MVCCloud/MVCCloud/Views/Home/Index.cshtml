@{
    ViewData["Title"] = "Home Page";
}
@model IEnumerable<Student>
<div >
    <h2 class="display-4" style="color: cadetblue; font-weight: bold;">Manager Employee</h2>
    <p>wellcome to my company !</p>
   

    <div class="text-center">
        <h1 class="display-4"><i class="fas fa-user-cog"></i> Employee in my company </h1>
        <p></p>
        <br />
        <div style="float:left">
            <input type="button" class="btn btn-primary"
                   value="List Employee"
                   onclick="location.href='@Url.Action("Index", "Home", new { orderBy = 0 })'" />

        </div>
        <p>
            <br />
            <br />
            @if (Model?.Count() > 0)
            {
                <table class="table table-bordered table-hover table-bordered">
                    <tr class="table-info">
                        <th>ID</th>
                        <th>Name </th>
                        <th>Year</th>
                        <th>Adress </th>

                        <th>Edit</th>


                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr class="table-light table-bordered">
                            <td>@item.id</td>
                            <td>@item.name</td>
                            <td>@item.year</td>

                            <td>@item.address</td>




                            <td>
                                @*<a href="/Home/GetEditStudent/@item.id"
                                title="EditBook">Edit</a>*@
                                @Html.ActionLink("Edit", "GetEditStudent", new { id = item.id })
                                |
                                @*<a href="/Home/GetEditStudent/@item.id"
                                title="EditBook">Edit</a>*@
                                @Html.ActionLink("Detail", "DetailStudent", new { id = item.id })
                            </td>
                        </tr>
                    }
                </table>}
            <!-- DataTables -->
            @*<script src="~/bower_components/datatables.net/js/jquery.dataTables.min.js"></script>
            <script src="~/bower_components/datatables.net-bs/js/dataTables.bootstrap.min.js"></script>*@
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
            <!-- page script -->
            @*<script>
                function Delete(id){


                    $.ajax(
                    {
                        type: "DELETE",
                        url: '@Url.Action("DeleteStudent", "Home")',
                        data: {
                            id: id
                        },
                        error: function (result) {
                            alert("error");
                        },
                        success: function (result) {
                            if (result == true) {
                                var baseUrl="/Home/DeleteStudent";
                                window.location.reload();
                            }
                            else {
                                alert("There is a problem, Try Later!");
                            }
                        }
                    });
                }

            </script>*@

    </div>
</div>
